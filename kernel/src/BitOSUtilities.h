#pragma once
char *itoa(int value, char *result, int base); // converts value to a string
bool isAlpha(char c); // checks if letter is lowercase or uppercase
char toUpper(char c); // converts lowercase character to uppercase
int strcmp(const char *s1, const char *s2); // compares two strings
size_t strlen(const char *str); // get length of null-terminated string
